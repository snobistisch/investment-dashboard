import type { TrackerEntry } from '../../types'

// Populated in Phase 1 from verified public sources only
// (@Citrini7 posts, free Substack tier, podcasts/interviews).
export const citriniEntries: TrackerEntry[] = []
